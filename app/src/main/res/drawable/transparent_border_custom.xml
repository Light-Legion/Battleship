<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Прозрачный фон -->
    <solid android:color="@android:color/transparent" />

    <!-- Обводка (граница) -->
    <stroke
        android:width="2dp"
        android:color="#000000" /> <!-- Цвет обводки -->

</shape>